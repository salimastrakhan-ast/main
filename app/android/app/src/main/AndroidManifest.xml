<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Доступ в сеть. Шаблон Flutter объявляет его только в debug- и
         profile-манифестах, поэтому релизная сборка без этой строки собирается
         молча и не открывает ни одного соединения: выглядит это как «сервер не
         отвечает», а не как отсутствующее разрешение. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Адресная книга: по ней ищутся знакомые, уже зарегистрированные в
         Tito. Наружу уходят только номера; имена остаются на устройстве.
         Спрашивается не при запуске, а когда человек открывает «Новый чат»:
         разрешение, о котором просят до того, как объяснили зачем, дают
         неохотно и потом жалеют. -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!-- Микрофон: голосовые сообщения и звонки. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Звонки. MODIFY_AUDIO_SETTINGS переключает вывод на разговорный
         динамик, BLUETOOTH_CONNECT — на гарнитуру. Служба переднего плана
         держит разговор живым, когда человек свернул приложение: с Android 14
         ей требуется явный тип, иначе система убивает её посреди звонка. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="Tito"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
