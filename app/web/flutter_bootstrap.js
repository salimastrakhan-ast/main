// Загрузчик Flutter с локальным CanvasKit.
//
// По умолчанию движок отрисовки тянется с www.gstatic.com. Для приложения,
// рассчитанного на Россию, это плохая зависимость: до чужого CDN может не
// быть доступа, и тогда вместо интерфейса человек видит белый экран. Файлы
// движка и так лежат в сборке — берём их оттуда.
{{flutter_js}}
{{flutter_build_config}}

_flutter.loader.load({
  config: {
    canvasKitBaseUrl: "canvaskit/",
  },
});
