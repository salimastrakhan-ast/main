# Depanini — что исправлено по ревью

Ревью: `DEPANINI-REVIEW.md` (39 находок). Здесь — что с ними сделано.

**Ветки с исправлениями:**

| Репозиторий | Ветка | Базовый коммит |
|---|---|---|
| `salimastrakhan-ast/depanini-backend` | `claude/depanini-fixes` | `a653b93` |
| `salimastrakhan-ast/depanini-app` | `claude/depanini-fixes` | `1c4b7c7` |

В этот раз доступ на запись к обоим репозиториям был, поэтому изменения
запушены ветками, а не отданы патчем.

## Проверено

```
backend: eslint — чисто, vitest — 84 passed (8 файлов), 4,6 с
app:     eslint — 0 ошибок, все 34 файла разбираются парсером
```

До правок было `1 failed | 69 passed`, 24 секунды. Разница во времени —
тесты больше не ходят в живой Nominatim: `tests/setup.js` глушит глобальный
`fetch`, и внешние сервисы уходят на свои фолбэк-ветки.

---

## Закрыто: 3 критичных

**C1 · XSS в админке → захват аккаунта администратора.**
Добавлена `esc()`, через неё проходит каждая подстановка в `cardHtml`,
`tripCard`, `renderDrivers`, `renderTariffs`. Скрипт вынесен из `index.html`
в `src/admin/admin.js`, поэтому CSP ужат с `default-src ... *` до
`script-src 'self'`; все `onclick` заменены делегированием по `data-action`.
Токен админки переехал из `localStorage` в `httpOnly`-куку (`SameSite=Strict`),
картинки грузятся `fetch`'ем с заголовком и показываются через `blob:` —
`?token=` из URL исчез.

**C2 · Сервер стартовал с зашитым JWT-секретом.**
Фолбэк `'dev-secret-change-me'` убран: без `JWT_SECRET` или с секретом короче
32 символов процесс не поднимается. Деплой проверяет переменную **до**
перезапуска PM2, чтобы не уронить работающий сервис. Есть регрессионный тест
на реальный запуск процесса.

**C3 · Любой клиент видел GPS любого водителя.**
`watch:driver` проверяет права так же, как соседний `join:order`: подписка
разрешена только при активном заказе с этим водителем (водителю — только на
себя). При терминальном статусе заказа подписка снимается автоматически.

## Закрыто: 10 высоких

| | Что сделано |
|---|---|
| H1 | `quote()` смотрит на `availability`, а не на `isOnline`; водитель занимается атомарным `UPDATE ... WHERE availability='online'` при создании заказа и освобождается на терминальных статусах. `isOnline` стал производным полем — разъехаться два поля больше не могут |
| H2 | `authLimiter` на `forgot-password`, `reset-password`, `verify-email`, `send-code`; отдельные колонки `reset_code`/`reset_expires`; счётчик попыток гасит код после 5 промахов; коды через `crypto.randomInt` |
| H3 | Отбор в фоновых задачах ушёл в SQL + два частичных индекса. Вместо чтения всей таблицы заказов каждые 5 секунд — один индексный запрос |
| H4 | Проверка дубля оценки (409 `ALREADY_RATED`), `UNIQUE(order_id)` продублирован в in-memory, чтобы тесты ловили расхождение |
| H5 | Удаление аккаунта в профиле приложения с двойным подтверждением |
| H6 | Геолокация через `expo-task-manager` с foreground-сервисом; убрана двойная отправка и утечка интервала |
| H7 | Haversine-фолбэк домножается на `DETOUR_FACTOR` (1.3), доля фолбэков видна в `/admin/stats` → `routing.fallbackRate` |
| H8 | RTL: `I18nManager`, логические отступы, зеркалирование иконок, шрифт Cairo |
| H9 | Лимит по идентификатору пользователя, а не по IP — CGNAT оператора больше не выключает сервис для целого сегмента города |
| H10 | Workflow с тестами, деплой через `needs: test`, тесты без сети |

## Закрыто: 17 средних и 9 низких

Полный список — в сообщениях коммитов. Кратко: дубли аккаунтов (M1), токен
в URL (M2), доступ к документам водителей (M3), таймаут заказа из настроек
(M4), версионированные миграции (M5), пагинация в админке (M6), точечный
запрос заказа (M7), устаревшее замыкание (M8), гонка при переборе (M9),
смена языка (M10), асинхронный scrypt (M11), комнаты вместо перебора сокетов
(M12), сеть против 401 при запуске (M13), телефоны водителей (M14), проверка
файлов по сигнатуре (M15), обнуление тарифа (M16), пакетные пуши (M17),
доступность (L1), линтер (L2), хуки (L3), заставка (L4), системная тема (L5),
словарь ошибок (L6), Firebase-конфиги (L7), журнал ошибок (L8),
документация (L9).

---

## Что нужно сделать руками при выкатке

**1. Миграции.** `npm run migrate` теперь версионированный: `schema.sql` —
только DDL, шаги лежат в `src/db/migrations/`, журнал — в таблице
`schema_migrations`. Прогнать **до** раскатки нового бэкенда.

**2. `JWT_SECRET` стал обязательным.** Если он не задан или короче 32
символов, сервер не стартует, а деплой останавливается до перезапуска PM2.
Проверьте `.env` на сервере заранее: `openssl rand -hex 32`.

**3. Сессии админки сбросятся.** Токен переехал в `httpOnly`-куку — админу
нужно войти заново. Учтите: кука ставится с `Secure` при `NODE_ENV=production`,
то есть админка должна открываться по HTTPS.

**4. Приложение и бэкенд надо выкатывать вместе.** Сервер больше не принимает
токен в query-строке, а приложение до этой версии грузило все картинки именно
так. Старые сборки перестанут показывать фото — сначала бэкенд, сразу следом
обновление приложения.

**5. `CORS_ORIGINS=*` в `.env.example` заменён на конкретный домен.**
Проверьте, что на сервере задано реальное значение.

**6. Новые зависимости приложения:** `expo-task-manager`, `expo-updates`,
`@expo-google-fonts/cairo`. Нужен `npm install` и новая нативная сборка —
фоновая геолокация требует изменений в манифестах, через OTA не приедет.

## Что осталось за пределами правок

- **Свой OSRM с картой Алжира.** Это разворачивание контейнера, а не правка
  кода. Пока не сделано — следите за `routing.fallbackRate` в `/admin/stats`:
  доля выше 5% означает, что заказы считаются по прямой и цена занижена.
- **S3 вместо локального диска (M15).** Файлы по-прежнему лежат рядом с кодом
  на одном инстансе. Переменные в `.env.example` заведены, но переключение
  требует миграции существующих файлов.
- **Sentry (L8).** Сделан локальный журнал ошибок как минимум; внешняя
  отчётность — отдельная задача с ключами и настройкой.
- **`google-services.json` нужно перевыпустить.** В закоммиченном файле
  массив `oauth_client` пуст — вход через Google на Android не заработает,
  пока в Firebase не добавлен SHA-1 отпечаток ключа подписи. Порядок описан
  в README приложения.
- **Серверная диспетчеризация (M9).** Перебор водителей защищён от гонки на
  клиенте, но сама логика по-прежнему живёт в приложении. Перенос на сервер —
  это изменение модели заказа, отдельная задача.
