-- Аватар пользователя.
--
-- Храним ключ объекта, а не готовую ссылку: ссылки на файлы подписанные и
-- живут шесть часов (media.linkTTL). Положи сюда ссылку — и аватар отвалится
-- к вечеру того же дня, причём у всех сразу и без единой ошибки в логах.
-- Поэтому ключ лежит в базе, а ссылка подписывается при каждой выдаче
-- профиля — ровно так же, как это уже сделано для вложений.
--
-- Прежняя колонка avatar_url остаётся: у чатов аватар пока задаётся ссылкой
-- извне, и ломать это ради пользователей незачем.
ALTER TABLE users ADD COLUMN avatar_key text;
